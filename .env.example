# =================================================================
# ENVIRONMENT VARIABLES PARA O PROJETO 'ONE'
# =================================================================
# Copie este arquivo para .env.local e preencha com seus valores

# -----------------------------------------------------------------
# SUPABASE CONFIGURATION
# -----------------------------------------------------------------
# Encontre estes valores em: https://app.supabase.com > Project Settings > API
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-supabase-service-role-key
SUPABASE_JWT_SECRET=your-supabase-jwt-secret # chave secreta JWT usada pelo Supabase

# -----------------------------------------------------------------
# NEXTAUTH CONFIGURATION  
# -----------------------------------------------------------------
# Para gerar NEXTAUTH_SECRET: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
NEXTAUTH_SECRET=your-nextauth-secret-64-characters
NEXTAUTH_URL=http://localhost:3000

# Para produção no Vercel, NEXTAUTH_URL é definido automaticamente
# Para Railway: NEXTAUTH_URL=https://your-app.railway.app

# -----------------------------------------------------------------
# OAUTH PROVIDERS (opcional - apenas se estiver usando)
# -----------------------------------------------------------------
# Google OAuth (Firebase Console > APIs & Services > Credentials)
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# GitHub OAuth (GitHub > Settings > Developer Settings > OAuth Apps)
GITHUB_CLIENT_ID=your-github-client-id
GITHUB_CLIENT_SECRET=your-github-client-secret

# -----------------------------------------------------------------
# DATABASE CONFIGURATION
# -----------------------------------------------------------------
# URL do banco (Supabase PostgreSQL ou outro)
DATABASE_URL=postgresql://user:password@host:port/database
POSTGRES_HOST=localhost            # host do banco PostgreSQL
POSTGRES_PORT=5432                 # porta do banco PostgreSQL
POSTGRES_DATABASE=postgres         # nome do banco de dados
POSTGRES_USER=postgres             # usuário do banco
POSTGRES_PASSWORD=password         # senha do banco
SUPABASE_NEON_NEON_DATABASE_URL=postgresql://user:password@host:port/database # string de conexão alternativa

# -----------------------------------------------------------------
# API CONFIGURATION
# -----------------------------------------------------------------
# URL da API externa mencionada no projeto
NEXT_PUBLIC_API_URL=https://api.agentesdeconversao.com.br

# -----------------------------------------------------------------
# ANALYTICS (opcional)
# -----------------------------------------------------------------
# Vercel Analytics (automaticamente configurado no Vercel)
# VERCEL_ANALYTICS_ID=your-analytics-id

# -----------------------------------------------------------------
# OUTROS (adicione conforme necessário)
# -----------------------------------------------------------------
# NODE_ENV=development
# PORT=3000

